{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "observation_common.schema.json",
  "title": "Shared Observation Provenance Contract",
  "description": "Provenance, period and missing-value semantics shared by every fact_* observation family (indicator, trade, port/transport, cost). This file defines no standalone record; it is referenced by the four observation schemas so that one change to provenance rules applies to all of them. Mode-neutral by construction: nothing here assumes Ocean, so Air, Road, Rail and Border modules reuse it unchanged.",
  "$defs": {
    "evidenceClass": {
      "description": "Evidence classification per docs/thailand_multimodal_logistics_intelligence_scope.md Section 3.5, extended with the two fixture classes required by WO-010 Section 8 so that non-live data can never be mistaken for a live published observation.",
      "enum": [
        "verified_fact",
        "official_publication",
        "official_notice",
        "official_forecast",
        "reported_claim",
        "analytical_inference",
        "scenario",
        "discovery_lead",
        "conflicting_evidence",
        "insufficient_evidence",
        "synthetic_test_fixture",
        "historical_validation_fixture"
      ]
    },
    "valueStatus": {
      "description": "Explicit missing-value status. Missing data must remain null or explicitly unavailable and must never be converted to zero: only 'available' permits a non-null value, and every other status forces value to be null.",
      "enum": [
        "available",
        "missing",
        "not_published",
        "suppressed",
        "retrieval_failed",
        "not_collected"
      ]
    },
    "periodType": {
      "enum": [
        "day",
        "week",
        "month",
        "quarter",
        "year",
        "point_in_time",
        "range"
      ]
    },
    "transportMode": {
      "description": "Mode reference. 'multimodal' and 'not_applicable' exist so shared entities never force an Ocean-only reading.",
      "enum": [
        "sea",
        "air",
        "road",
        "rail",
        "border",
        "inland_waterway",
        "multimodal",
        "not_applicable"
      ]
    },
    "provenance": {
      "type": "object",
      "description": "Required provenance block. Every published material fact must be traceable to a source, a data period, a publication time and a retrieval time.",
      "additionalProperties": false,
      "required": [
        "record_id",
        "source_id",
        "source_record_id",
        "dataset",
        "evidence_origin",
        "retrieval_status",
        "content_hash_scope",
        "period_start",
        "period_end",
        "period_type",
        "published_at",
        "retrieved_at",
        "revised_at",
        "revision_number",
        "content_sha256",
        "parser_version",
        "source_revision",
        "evidence_class",
        "known_limitations"
      ],
      "properties": {
        "record_id": {
          "type": "string",
          "pattern": "^OBS-[A-Z0-9_]+-[a-z0-9_]+-[0-9A-Za-z_:.-]+$",
          "description": "Stable record ID. Deterministic for a given source, series and period so that re-collection updates rather than duplicates."
        },
        "source_id": {
          "type": "string",
          "pattern": "^[A-Z0-9_]+$",
          "description": "The source the record actually came from. A generated record must use the reserved identifier SYNTHETIC_FIXTURE and record the publisher it stands in for in intended_source_id."
        },
        "source_record_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The source's own identifier for this record where one exists. Null when the source publishes no per-record identifier -- null is an honest absence, not an empty string."
        },
        "period_start": {
          "type": [
            "string",
            "null"
          ],
          "format": "date"
        },
        "period_end": {
          "type": [
            "string",
            "null"
          ],
          "format": "date"
        },
        "period_type": {
          "$ref": "#/$defs/periodType"
        },
        "published_at": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time",
          "description": "When the source published this value. Null when the source states no publication time."
        },
        "retrieved_at": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time",
          "description": "When the source response was actually retrieved. Null unless retrieval_status is 'retrieved'."
        },
        "revised_at": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time",
          "description": "When the source last revised this value, when the source exposes revision information."
        },
        "revision_number": {
          "type": "integer",
          "minimum": 0,
          "description": "Monotonic counter incremented when a later collection changes the value for the same record_id. Revisions are preserved as separate history rows, never silently overwritten."
        },
        "content_sha256": {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$"
        },
        "parser_version": {
          "type": "string",
          "pattern": "^[a-z0-9_]+_v[0-9]+$"
        },
        "source_revision": {
          "type": [
            "string",
            "null"
          ]
        },
        "evidence_class": {
          "$ref": "#/$defs/evidenceClass"
        },
        "known_limitations": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "evidence_origin": {
          "$ref": "#/$defs/evidenceOrigin"
        },
        "retrieval_status": {
          "$ref": "#/$defs/retrievalStatus"
        },
        "content_hash_scope": {
          "$ref": "#/$defs/contentHashScope"
        },
        "intended_source_id": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^[A-Z0-9_]+$",
          "description": "For a fixture, the production source candidate it stands in for. Must resolve in config/sources.yaml. Never a claim that the record came from that source."
        },
        "fixture_created_at": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time",
          "description": "When the fixture was generated. Recorded instead of, never as, a publication or retrieval time."
        },
        "dataset": {
          "$ref": "#/$defs/dataset",
          "description": "The publication surface this record belongs to, carried at record level so a record copied into a package takes its surface with it rather than inheriting one from whatever file it lands in."
        },
        "collection_run_id": {
          "type": ["string", "null"],
          "pattern": "^COL-[0-9]{8}T[0-9]{6}Z-[A-Z0-9_]+$",
          "description": "WO-010-R5 §1. Required (non-null) when evidence_origin is 'live_retrieved': the persisted collection-run manifest this record was actually emitted from. Optional so fixture records, which carry no acquisition event, are unaffected; enforcement that a live_retrieved record without one cannot reach current publication happens in analysis.provenance.acquisition_binding_problems, not here."
        },
        "manual_review_event_id": {
          "type": ["string", "null"],
          "pattern": "^MAN-[0-9]{8}T[0-9]{6}Z-[A-Z0-9_]+$",
          "description": "WO-010-R5 §1. Required (non-null) when evidence_origin is 'human_reviewed_manual': the persisted manual-review event this record's transcription is the basis for. Optional for the same reason as collection_run_id."
        }
      },
      "allOf": [
        {
          "description": "WO-010-R7-R1: makes the retrieved_at description above ('Null unless retrieval_status is retrieved') structural rather than prose-only, matching schemas/event_evidence.schema.json and analysis.provenance.provenance_problems.",
          "if": {
            "required": ["retrieval_status"],
            "properties": {"retrieval_status": {"const": "retrieved"}}
          },
          "then": {
            "required": ["retrieved_at"],
            "properties": {"retrieved_at": {"type": "string"}}
          }
        },
        {
          "if": {
            "required": ["retrieval_status"],
            "properties": {"retrieval_status": {"enum": ["not_retrieved", "retrieval_failed", "not_applicable"]}}
          },
          "then": {
            "properties": {"retrieved_at": {"type": "null"}}
          }
        }
      ]
    },
    "measurement": {
      "type": "object",
      "description": "Value, unit and missing-value semantics. Enforced by scripts/validate.py: value is non-null if and only if value_status is 'available'.",
      "additionalProperties": false,
      "required": [
        "value",
        "value_status",
        "unit",
        "currency"
      ],
      "properties": {
        "value": {
          "type": [
            "number",
            "null"
          ]
        },
        "value_status": {
          "$ref": "#/$defs/valueStatus"
        },
        "unit": {
          "type": [
            "string",
            "null"
          ],
          "description": "Unit of measure, e.g. 'THB_million', 'USD', 'teu', 'index_points', 'port_calls', 'THB_per_litre'. Null only when value_status is not 'available'."
        },
        "currency": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^[A-Z]{3}$",
          "description": "ISO 4217 currency for monetary values; null for non-monetary series."
        }
      }
    },
    "placement": {
      "type": "object",
      "description": "Where the observation applies. Every field is nullable because a valid observation may be country-level with no node, or node-level with no lane.",
      "additionalProperties": false,
      "required": [
        "geography_id",
        "country_id",
        "transport_mode",
        "lane_id",
        "node_id"
      ],
      "properties": {
        "geography_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "country_id": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^[A-Z]{2}$"
        },
        "transport_mode": {
          "$ref": "#/$defs/transportMode"
        },
        "lane_id": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^LANE-[A-Z0-9-]+$"
        },
        "node_id": {
          "type": [
            "string",
            "null"
          ],
          "pattern": "^NODE-[A-Z0-9-]+$"
        }
      }
    },
    "evidenceOrigin": {
      "description": "Where the record came from. Only 'live_retrieved' and 'human_reviewed_manual' may carry current Logistics intelligence; the two fixture origins exercise the engine and are confined to the technical-demonstration and historical-validation surfaces.",
      "enum": [
        "live_retrieved",
        "human_reviewed_manual",
        "synthetic_test_fixture",
        "historical_validation_fixture"
      ]
    },
    "retrievalStatus": {
      "description": "Whether anything was actually fetched from a publisher. 'not_retrieved' forbids a retrieved_at timestamp: a retrieval time on a record nothing was retrieved for is false.",
      "enum": [
        "retrieved",
        "not_retrieved",
        "retrieval_failed",
        "not_applicable"
      ]
    },
    "contentHashScope": {
      "description": "What content_sha256 actually covers. 'authored_claim_record' means the hash is over this repository's own authored text and is NOT evidence about any publisher response.",
      "enum": [
        "source_response",
        "local_fixture_payload",
        "authored_claim_record"
      ]
    },
    "dataset": {
      "description": "Which publication surface the record belongs to. Only 'current_publication' reaches the Dashboard's current-intelligence view.",
      "enum": [
        "current_publication",
        "technical_demo",
        "historical_validation"
      ]
    }
  }
}
