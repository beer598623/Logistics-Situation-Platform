# Human-approved AI assessments

`scripts/review_decision.py --decision approve` writes approved assessments
here. The Dashboard's *AI Outlook and Preparedness* section reads only this
directory, so an unreviewed assessment can never be published.

This directory is empty by design: no AI assessment has been produced or
approved under WO-010. The Dashboard states that on its face rather than
showing an empty panel.
