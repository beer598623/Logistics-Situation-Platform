# Superseded assessments

`scripts/review_decision.py` moves the previously approved assessment here
before writing a new one, so a prior view is preserved rather than silently
rewritten. Every move is also recorded in
`data/assessments/assessment_history.json`.
